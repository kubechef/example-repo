[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "checkout-anomaly-lab"
version = "0.1.0"
description = "An intentionally failing checkout boundary fixture for NAYR diagnostics"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

